% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lognormal.R
\name{lognormal_lowercase}
\alias{lognormal_lowercase}
\alias{dlognormalmix}
\alias{plognormalmix}
\alias{qlognormalmix}
\alias{rlognormalmix}
\alias{dlognormalmixgpd}
\alias{plognormalmixgpd}
\alias{qlognormalmixgpd}
\alias{rlognormalmixgpd}
\alias{dlognormalgpd}
\alias{plognormalgpd}
\alias{qlognormalgpd}
\alias{rlognormalgpd}
\title{Lowercase vectorized lognormal distribution functions}
\usage{
dlognormalmix(x, w, meanlog, sdlog, log = FALSE)

plognormalmix(q, w, meanlog, sdlog, lower.tail = TRUE, log.p = FALSE)

qlognormalmix(
  p,
  w,
  meanlog,
  sdlog,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)

rlognormalmix(n, w, meanlog, sdlog)

dlognormalmixgpd(
  x,
  w,
  meanlog,
  sdlog,
  threshold,
  tail_scale,
  tail_shape,
  log = FALSE
)

plognormalmixgpd(
  q,
  w,
  meanlog,
  sdlog,
  threshold,
  tail_scale,
  tail_shape,
  lower.tail = TRUE,
  log.p = FALSE
)

qlognormalmixgpd(
  p,
  w,
  meanlog,
  sdlog,
  threshold,
  tail_scale,
  tail_shape,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)

rlognormalmixgpd(n, w, meanlog, sdlog, threshold, tail_scale, tail_shape)

dlognormalgpd(
  x,
  meanlog,
  sdlog,
  threshold,
  tail_scale,
  tail_shape,
  log = FALSE
)

plognormalgpd(
  q,
  meanlog,
  sdlog,
  threshold,
  tail_scale,
  tail_shape,
  lower.tail = TRUE,
  log.p = FALSE
)

qlognormalgpd(
  p,
  meanlog,
  sdlog,
  threshold,
  tail_scale,
  tail_shape,
  lower.tail = TRUE,
  log.p = FALSE
)

rlognormalgpd(n, meanlog, sdlog, threshold, tail_scale, tail_shape)
}
\arguments{
\item{x}{Numeric vector of quantiles.}

\item{w}{Numeric vector of mixture weights.}

\item{meanlog, sdlog}{Numeric vectors (mix) or scalars (base+gpd) of component parameters.}

\item{log}{Logical; if \code{TRUE}, return log-density.}

\item{q}{Numeric vector of quantiles.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le x)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are on log scale.}

\item{p}{Numeric vector of probabilities.}

\item{tol, maxiter}{Tolerance and max iterations for numerical inversion.}

\item{n}{Integer number of observations to generate.}

\item{threshold, tail_scale, tail_shape}{GPD tail parameters (scalars).}
}
\value{
Numeric vector of densities, probabilities, quantiles, or random variates.
}
\description{
Vectorized R wrappers for the scalar lognormal-kernel topics in this file.
}
\details{
These are direct vectorized wrappers around the scalar lognormal routines. They keep the same
parameterization, support restrictions, and bulk-tail splice, while allowing ordinary vector
inputs in R. Quantile wrappers continue to use the scalar inversion logic, so there is no
separate approximation layer in the lowercase API.
}
\section{Functions}{
\itemize{
\item \code{dlognormalmix()}: Lognormal mixture density (vectorized)

\item \code{plognormalmix()}: Lognormal mixture distribution function (vectorized)

\item \code{qlognormalmix()}: Lognormal mixture quantile function (vectorized)

\item \code{rlognormalmix()}: Lognormal mixture random generation (vectorized)

\item \code{dlognormalmixgpd()}: Lognormal mixture + GPD density (vectorized)

\item \code{plognormalmixgpd()}: Lognormal mixture + GPD distribution function (vectorized)

\item \code{qlognormalmixgpd()}: Lognormal mixture + GPD quantile function (vectorized)

\item \code{rlognormalmixgpd()}: Lognormal mixture + GPD random generation (vectorized)

\item \code{dlognormalgpd()}: Lognormal + GPD density (vectorized)

\item \code{plognormalgpd()}: Lognormal + GPD distribution function (vectorized)

\item \code{qlognormalgpd()}: Lognormal + GPD quantile function (vectorized)

\item \code{rlognormalgpd()}: Lognormal + GPD random generation (vectorized)

}}
\examples{
w <- c(0.6, 0.3, 0.1)
ml <- c(0, 0.3, 0.6)
sl <- c(0.4, 0.5, 0.6)

# Lognormal mixture
dlognormalmix(c(1, 2, 3), w = w, meanlog = ml, sdlog = sl)
rlognormalmix(5, w = w, meanlog = ml, sdlog = sl)

# Lognormal mixture + GPD
dlognormalmixgpd(c(2, 3, 4), w = w, meanlog = ml, sdlog = sl,
                 threshold = 2.5, tail_scale = 0.5, tail_shape = 0.2)

}
\seealso{
\code{\link[=lognormal_mix]{lognormal_mix()}}, \code{\link[=lognormal_mixgpd]{lognormal_mixgpd()}}, \code{\link[=lognormal_gpd]{lognormal_gpd()}}, \code{\link[=bundle]{bundle()}},
\code{\link[=get_kernel_registry]{get_kernel_registry()}}.

Other vectorized kernel helpers: 
\code{\link{amoroso_lowercase}},
\code{\link{base_lowercase}},
\code{\link{cauchy_mix_lowercase}},
\code{\link{gamma_lowercase}},
\code{\link{invgauss_lowercase}},
\code{\link{laplace_lowercase}},
\code{\link{normal_lowercase}}
}
\concept{vectorized kernel helpers}
