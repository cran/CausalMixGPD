% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{print.mixgpd_summary}
\alias{print.mixgpd_summary}
\title{Print a MixGPD summary object}
\usage{
\method{print}{mixgpd_summary}(x, digits = 3, max_rows = 60, show_ess = FALSE, ...)
}
\arguments{
\item{x}{A \code{"mixgpd_summary"} object.}

\item{digits}{Number of digits to print.}

\item{max_rows}{Maximum rows to print.}

\item{show_ess}{Logical; if \code{TRUE}, include the \code{ess} column when present.}

\item{...}{Unused.}
}
\value{
\code{x} invisibly.
}
\description{
Print a MixGPD summary object
}
\details{
This method formats the output of \code{summary.mixgpd_fit()}. It prints the model
metadata, any stored WAIC value, the effective truncation information induced
by \code{epsilon}, and the parameter-level posterior summary table.

The printed rows correspond to monitored posterior parameters. They are not
predictions of densities, quantiles, or means, which should instead be
obtained from \code{predict.mixgpd_fit()}.
}
\examples{
\donttest{
y <- abs(stats::rnorm(25)) + 0.1
bundle <- build_nimble_bundle(y = y, backend = "sb", kernel = "normal",
                             GPD = TRUE, components = 3,
                             mcmc = list(niter = 100, nburnin = 50, thin = 1, nchains = 1))
fit <- run_mcmc_bundle_manual(bundle)
summary(fit)
}
}
