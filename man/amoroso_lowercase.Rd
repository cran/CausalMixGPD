% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/amoroso.R
\name{amoroso_lowercase}
\alias{amoroso_lowercase}
\alias{damorosomix}
\alias{pamorosomix}
\alias{qamorosomix}
\alias{ramorosomix}
\alias{damorosomixgpd}
\alias{pamorosomixgpd}
\alias{qamorosomixgpd}
\alias{ramorosomixgpd}
\alias{damorosogpd}
\alias{pamorosogpd}
\alias{qamorosogpd}
\alias{ramorosogpd}
\title{Lowercase vectorized Amoroso distribution functions}
\usage{
damorosomix(x, w, loc, scale, shape1, shape2, log = FALSE)

pamorosomix(q, w, loc, scale, shape1, shape2, lower.tail = TRUE, log.p = FALSE)

qamorosomix(
  p,
  w,
  loc,
  scale,
  shape1,
  shape2,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)

ramorosomix(n, w, loc, scale, shape1, shape2)

damorosomixgpd(
  x,
  w,
  loc,
  scale,
  shape1,
  shape2,
  threshold,
  tail_scale,
  tail_shape,
  log = FALSE
)

pamorosomixgpd(
  q,
  w,
  loc,
  scale,
  shape1,
  shape2,
  threshold,
  tail_scale,
  tail_shape,
  lower.tail = TRUE,
  log.p = FALSE
)

qamorosomixgpd(
  p,
  w,
  loc,
  scale,
  shape1,
  shape2,
  threshold,
  tail_scale,
  tail_shape,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)

ramorosomixgpd(
  n,
  w,
  loc,
  scale,
  shape1,
  shape2,
  threshold,
  tail_scale,
  tail_shape
)

damorosogpd(
  x,
  loc,
  scale,
  shape1,
  shape2,
  threshold,
  tail_scale,
  tail_shape,
  log = FALSE
)

pamorosogpd(
  q,
  loc,
  scale,
  shape1,
  shape2,
  threshold,
  tail_scale,
  tail_shape,
  lower.tail = TRUE,
  log.p = FALSE
)

qamorosogpd(
  p,
  loc,
  scale,
  shape1,
  shape2,
  threshold,
  tail_scale,
  tail_shape,
  lower.tail = TRUE,
  log.p = FALSE,
  tol = 1e-10,
  maxiter = 200
)

ramorosogpd(n, loc, scale, shape1, shape2, threshold, tail_scale, tail_shape)
}
\arguments{
\item{x}{Numeric vector of quantiles.}

\item{w}{Numeric vector of mixture weights.}

\item{loc, scale, shape1, shape2}{Numeric vectors (mix) or scalars (base+gpd) of component parameters.}

\item{log}{Logical; if \code{TRUE}, return log-density.}

\item{q}{Numeric vector of quantiles.}

\item{lower.tail}{Logical; if \code{TRUE} (default), probabilities are \eqn{P(X \le x)}.}

\item{log.p}{Logical; if \code{TRUE}, probabilities are on log scale.}

\item{p}{Numeric vector of probabilities.}

\item{tol, maxiter}{Tolerance and max iterations for numerical inversion.}

\item{n}{Integer number of observations to generate.}

\item{threshold, tail_scale, tail_shape}{GPD tail parameters (scalars).}
}
\value{
Numeric vector of densities, probabilities, quantiles, or random variates.
}
\description{
Vectorized R wrappers for the scalar Amoroso-kernel topics in this file.
}
\details{
These are vectorized wrappers around the scalar Amoroso routines used internally by the package.
They preserve the same location-scale-shape parameterization and the same piecewise splice logic
for GPD tails. Quantile wrappers therefore continue to rely on the scalar numerical inversion or
scalar GPD inverse exactly as documented for the uppercase functions.
}
\section{Functions}{
\itemize{
\item \code{damorosomix()}: Amoroso mixture density (vectorized)

\item \code{pamorosomix()}: Amoroso mixture distribution function (vectorized)

\item \code{qamorosomix()}: Amoroso mixture quantile function (vectorized)

\item \code{ramorosomix()}: Amoroso mixture random generation (vectorized)

\item \code{damorosomixgpd()}: Amoroso mixture + GPD density (vectorized)

\item \code{pamorosomixgpd()}: Amoroso mixture + GPD distribution function (vectorized)

\item \code{qamorosomixgpd()}: Amoroso mixture + GPD quantile function (vectorized)

\item \code{ramorosomixgpd()}: Amoroso mixture + GPD random generation (vectorized)

\item \code{damorosogpd()}: Amoroso + GPD density (vectorized)

\item \code{pamorosogpd()}: Amoroso + GPD distribution function (vectorized)

\item \code{qamorosogpd()}: Amoroso + GPD quantile function (vectorized)

\item \code{ramorosogpd()}: Amoroso + GPD random generation (vectorized)

}}
\examples{
w <- c(0.6, 0.3, 0.1)
locs <- c(0.5, 0.5, 0.5)
scls <- c(1, 1.3, 1.6)
s1 <- c(2.5, 3, 4)
s2 <- c(1.2, 1.2, 1.2)

# Amoroso mixture
damorosomix(c(1, 2, 3), w = w, loc = locs, scale = scls, shape1 = s1, shape2 = s2)
ramorosomix(5, w = w, loc = locs, scale = scls, shape1 = s1, shape2 = s2)

}
\seealso{
\code{\link[=amoroso_mix]{amoroso_mix()}}, \code{\link[=amoroso_mixgpd]{amoroso_mixgpd()}}, \code{\link[=amoroso_gpd]{amoroso_gpd()}}, \code{\link[=bundle]{bundle()}},
\code{\link[=get_kernel_registry]{get_kernel_registry()}}.

Other vectorized kernel helpers: 
\code{\link{base_lowercase}},
\code{\link{cauchy_mix_lowercase}},
\code{\link{gamma_lowercase}},
\code{\link{invgauss_lowercase}},
\code{\link{laplace_lowercase}},
\code{\link{lognormal_lowercase}},
\code{\link{normal_lowercase}}
}
\concept{vectorized kernel helpers}
